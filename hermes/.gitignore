.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.coverage
htmlcov/
.env
profiles/*/sessions/
profiles/*/logs/
profiles/*/auth.json
profiles/*/auth.lock
profiles/*/*.cache*
profiles/*/models_dev_cache.json

# Continuous-learning corpus & eval outputs — raw user data (PII), never commit
data/corpus/
metrics/

# Prompt-optimization harness (CL Phase 1 Slice 3): generated replies may echo
# user_text, and the disk cache / run artifacts are per-run scratch — never commit.
eval/prompt_opt/cache/
eval/prompt_opt/runs/
profiles/*/state.db

# Puzzle DB built from the Lichess dump (scripts/import_puzzles.py) — 2 GB, never commit
data/puzzles.db
