# Flask core
Flask==3.1.0
flask-cors==5.0.1
python-dotenv
Werkzeug==3.1.3
gunicorn==26.0.0

# Phase 1: LLM Orchestration & Learning Platform
openai
anthropic
supabase
pyjwt  # For Clerk JWT verification

# ETL pipeline dependencies (Phase 2)
# weaviate-client
# langgraph
# langchain
# sentence-transformers

# Redis (Phase 2)
# redis

# Development & Testing
pytest
pytest-mock
rich  # For better logging

# Note: Stockfish, whisper, and elevenlabs removed
# - Stockfish analysis now handled by frontend WASM
# - Voice features deferred to future phase
requests

# Server-side Maia bot fallback (frontend plays instantly before ONNX download finishes)
# Pinned to versions with a stable numpy/protobuf ABI (newer combos segfault on model load).
onnxruntime==1.20.1
numpy<2
python-chess  # legal-move generation for Maia preprocessing
